export const metadata = {
  title: "Dasboard",
};

const layout = ({ children }) => {
  return (
    <div>
      <h1>SAYA ADALAH LAYOUT DASHBOARD</h1>
      {children}
    </div>
  );
};

export default layout;
